
  <h1>All the Reviews </h1>

<ul>
<% @reviews.each do |rw| %>
<li>
    <%= rw.shoe.shoe_type%>: <%= link_to rw.title, shoe_reviews_path(rw.shoe, rw)%> - <%= rw.stars%> 
    <br>
    <%= rw.shoe.brand.name%>
     <p><%=rw.content%></p>
    
    <%= link_to 'Edit', edit_shoe_review_path(rw.shoe, rw)%>
    <%#=link_to 'Destroy', shoe_path(rw.id),  method:  :delete %>
  </li> <br>
<% end %>
</ul>